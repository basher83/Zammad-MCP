# .envrc

CODACY_API_TOKEN=$(infisical secrets get CODACY_API_KEY --path=/API-keys --plain 2>/dev/null)
if [ -z "$CODACY_API_TOKEN" ]; then
  echo "⚠️  CODACY_API_TOKEN could not be retrieved – skipping export." >&2
else
  export CODACY_API_TOKEN
fi

export CODACY_ORGANIZATION_PROVIDER=gh
export CODACY_USERNAME=basher83
export CODACY_PROJECT_NAME=Zammad-MCP