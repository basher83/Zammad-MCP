**Zammad MCP** — Integrate Zammad ticketing system as an MCP server, enabling AI assistants to query, create, and manage support tickets natively.

- Expose Zammad's REST API as MCP tools: search tickets, fetch ticket details, create/update tickets, add notes
- Implement resource handlers for user and organization data retrieval
- Support ticket state transitions and assignee management via tool calls
- Add notification polling for real-time ticket updates
- Include authentication via Zammad API token with secure credential handling
